namespace Discord
{
    public enum ImageFormat
    {
        /// <summary>
        /// Resolve this image as either a PNG or GIF, depending on if the avatar is animated.
        /// </summary>
        Auto,
        WebP,
        Png,
        Jpeg,
        Gif,
    }
}
